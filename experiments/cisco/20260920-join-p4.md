# P4 实验报告：Join 阶段会话模糊测试（2026-09-20）

计划依据：`docs/fuzzer-全链路化升级计划-20260920.md` P4——"Join 变异上设备（首次设备授权，1 天在环）"。

## 会话五要素

| 项 | 值 |
| --- | --- |
| commit | `4beae52`（P4 代码）+ `6afca81`/`383e79e`（在环修正与按文档流程对齐的审计修正：transport/变体/unlocked/时间戳）+ `430aa59`（本报告）；分支 `feature/session-fuzzing` |
| seed | 定向矩阵：变体名即变异描述（无需随机）；未锁定对照：`--seed 20260920` |
| 参数 | `--ac-ip 192.168.10.201`，`--model C9105AXI-C`，`--round-gap 1`，retries=1；证书 `e2ap4.crt/key`（路径传参，不入仓） |
| 目标/时间 | C9800-fresh（C9800-CL 17.14.01），2026-09-20 10:00–13:00Z（含调试轮） |
| 原始数据 | Ubuntu `~/projects/capwap_log/20260920_join_p4/`：`base10/`、`locked20/`、`matrix/`、`unlocked200/`（各含 `session.jsonl`+`summary.json`+运行日志；`unlocked200.rounds.tsv` 为逐轮完成时刻记录，用于与设备侧 btrace 对时间）。**原始数据保留在 Ubuntu，不随本报告入仓**——本行此前写"副本随报告提交至 `experiments/cisco/20260920-join-p4/`"，该目录从未创建，2026-09-20 更正 |

环境：LB 闸门补丁在位、cfmgr 已还原原始字节（首轮曾误重打全零补丁 → "Failure encoding auth token payload"，已修正并记入下方教训）、profile/global country CN、bf-agent 在位。

## 1. 运行模式

- **每轮独立 DTLS 会话**：Discovery 前奏（同 5 元组，**并消费 Discovery Response**）→ 握手（等飞行闭合 + 1.5s 沉降）→ Join Request → 15s 等待 → 优雅关闭（stdin EOF → close_notify）；
- **retries=1**：非 ANSWERED 时换新会话重试一次（见 §3）；
- 判定：Result Code（Type 33）/ alert（s_client 被服务端关闭）/ silence / btrace disjoin 抽样核对。

## 2. 基线（locked join-v1 = base 变体）

| 会话 | 轮数 | 结果 |
| --- | --- | --- |
| `base10` | 10 | **10/10 Result Code = 0** |
| `locked20` | 20 | **20/20 Result Code = 0**（attempts 直方图：1 轮首次即中，19 轮第二次尝试命中） |

base = 黄金参数（radio 0/1、model C9105AXI-C、Type 29=14400、join 时 126 码 0x0101、AP Domain "default"、全部 VSP、Session ID 随机）。

## 3. 调试中发现的环境事实（fuzzer 行为依据）

1. **成功后短时间内新握手被吞**（旧称"同 5 元组压制"）：固定源端口连发时，首轮 rc=0 后第 2–4 轮全静默；btrace 显示 Join 阶段会话约 **32s** 心跳超时释放。**机制尚无定论**，现有三组观察互不排斥：(a) 控制器为同一 AP 保留上一个会话的记录，直到新 DTLS 会话建立或心跳过期（RFC 5415 §12.3，`docs/evidence/rfc/rfc5415.txt:7630-7634`）；(b) 同节允许在 `MaxFailedDTLSSessionRetry`（默认 3，§4.8.6）后对新 DTLS 握手限速，且**建议采用随机丢弃**（`rfc5415.txt:7643-7651`）——这与"换新源端口仍有零头被吞"吻合；(c) 客户端侧未干净解链也会把会话占住（旧 `tools/join_session.py`/`tools/e2_join_chain.py` 带 `-ign_eof` 且退出直接 kill）。三者需用"同 AP MAC + 固定端口 vs 换端口"的分离实验区分（工具已备：`tools/fixed_port_test.py`）。**注意 32s 属 Join 阶段**，与文档中注册/Run 阶段的 ~90s 不是同一个值（后者已被 E7 的常驻应答线程消除）；
2. **换端口重试有效**：临时源端口下首次尝试大概率落在上一会话存活窗口内被丢，第二次尝试（新会话≈20s 后）稳定命中——`locked20` 的 19/20 二次命中即此形态；
3. **cfmgr 教训**：P4 bring-up 时误跑含 cfmgr 全零补丁的 `patch.sh` → join 全部 "Failure encoding auth token payload"（E5 已知），改用 `revert_cfmgr2.sh` 还原后恢复正常；patch.sh 的 cfmgr 段在该实例上禁用。

## 4. 定向变体矩阵（16 变体 × 2 轮，`matrix/`）

| 变体 | 结果 | 归因 |
| --- | --- | --- |
| base | 2/2 rc=0 | 基线 |
| omit-126 | 2/2 rc=0 | join 时 Type 126 不参与 join 准入（生效点在 Config Status 的 VSP-126） |
| omit-169 | 2/2 rc=0 | AP Domain 缺失不拒（仅 "AP_DOMAIN payload count is 0"） |
| **omit-37** | **2/2 alert** | VSP（board options/RAD_NAME）为 join 必需 |
| **omit-29** | **2/2 alert** | Maximum Message Length 必需（复现 run69 的 "Failure decoding max message size"） |
| omit-53 | 2/2 rc=0 | Limited ECN 可省 |
| omit-30 | 2/2 rc=0 | WTP IPv4 Address 可省 |
| maxmsglen-0 / -65535 | 2/2 rc=0 | Type 29 只查存在性、不查取值 |
| **radio-1base** | **2/2 alert** | Radio ID 必须 0 基（slot 0 必须存在，复现 E5 结论） |
| radio-type-b-a | 2/2 rc=0 | 类型位图走自愈默认 |
| regdom-code-0 / FFFF | 2/2 rc=0 | join 时域码取值不校验 |
| **boarddata-shift** | **2/2 silence** | 板数据子元素编号 +1 → 整消息静默丢弃；btrace 见 **MAC 0000.0000.0000** 会话（身份解析失败），无回包且连接不关闭 |
| session-zero | （本轮补入，见下一批） | Session ID 全零 |
| session-short8 | （本轮补入，见下一批） | Session ID 元素线缆级截断为 8B |

## 5. btrace 抽样核对（`show logging process wncd to-file`）

矩阵窗口 10:11–10:19Z 解码：

- alert 轮（omit-37/29、radio-1base）：`Internal Error, Phase: Join`（10:14:31–10:14:45 三条，与轮次时间窗吻合）；
- 接受轮：`Heart beat timer expiry, Phase: Join`（~32s 后，客户端已离场）；
- boarddata-shift 轮：`MAC 0000.0000.0000 ... Heart beat timer expiry`——证实"身份被解析为空"而非消息级拒绝。

## 6. 结论与后续

1. P4 验收项：锁定基线 ✅（20/20 rc=0）、变体矩阵 ✅（16 变体全部可归因）、报告 ✅（本文件）；
2. 未锁定对照 200 轮（`--variants unlocked --seed 20260920`）运行中，冻结集 {38,35,29,1048} 逐字节保持，其余元素随机 1 处变异（flip/zero/truncate/drop），结果写入 `unlocked200/`；
3. 拟办（P5 前置）：把 32s 会话窗口写进 `statemachine.py` 常量与 runbook 排查注意；200 轮若暴露新的静默/拒绝簇，按 `oracle.py` 映射表补充归因条目。
